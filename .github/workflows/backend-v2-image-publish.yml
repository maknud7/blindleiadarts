name: Backend v2 Image Publish

on:
  pull_request:
    branches:
      - develop
    paths:
      - 'apps/backend-v2/**'
      - 'package.json'
      - '.dockerignore'
      - '.github/workflows/backend-v2-image-publish.yml'
  push:
    branches:
      - develop
    paths:
      - 'apps/backend-v2/**'
      - 'package.json'
      - '.dockerignore'
      - '.github/workflows/backend-v2-image-publish.yml'
  workflow_dispatch:

permissions:
  contents: read
  packages: write

concurrency:
  group: backend-v2-image-publish-${{ github.ref }}
  cancel-in-progress: true

jobs:
  candidate-image:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set image name
        shell: bash
        run: |
          set -Eeuo pipefail
          echo "IMAGE=ghcr.io/${GITHUB_REPOSITORY_OWNER,,}/blindleia-backend-v2" >> "$GITHUB_ENV"
          echo "SHA_TAG=sha-${GITHUB_SHA}" >> "$GITHUB_ENV"

      - name: Login to GHCR
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build candidate image
        uses: docker/build-push-action@v6
        with:
          context: .
          file: apps/backend-v2/Dockerfile
          push: ${{ github.event_name != 'pull_request' }}
          tags: |
            ${{ env.IMAGE }}:${{ env.SHA_TAG }}
            ${{ env.IMAGE }}:develop
          labels: |
            org.opencontainers.image.source=https://github.com/${{ github.repository }}
            org.opencontainers.image.revision=${{ github.sha }}
            org.opencontainers.image.title=Blindleia Backend v2
          cache-from: type=gha
          cache-to: type=gha,mode=max

      - name: Record candidate result
        shell: bash
        run: |
          set -Eeuo pipefail
          {
            echo "### Backend v2 container candidate"
            echo
            if [ "${GITHUB_EVENT_NAME}" = "pull_request" ]; then
              echo "Image built successfully without registry login or push."
            else
              echo "Immutable image: \`${IMAGE}:${SHA_TAG}\`"
              echo
              echo "The mutable \`${IMAGE}:develop\` tag points to the same candidate."
            fi
            echo
            echo "This workflow never deploys the image or enables PROD writes."
          } >> "$GITHUB_STEP_SUMMARY"
